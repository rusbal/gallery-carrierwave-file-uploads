
<%= render 'layouts/errors', obj: @painting %>

<%= form_for [@gallery, @painting], html: { multipart: true } do |f| %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.file_field :image %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>
